# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Unity WebGL build
# Por ahora (entrega academica) se commitea el build para que el deploy lo tenga.
# A futuro, cuando movamos el build a un CDN (opcion C), se vuelven a ignorar
# estas carpetas descomentando las lineas de abajo.
# public/unity-build/Build/
# public/unity-build/TemplateData/
# public/unity-build/index.html
# public/unity-build/StreamingAssets/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Carpeta donde Unity escupe los builds crudos (no se sube al repo)
unity-builds/